export { globalError } from "@middlewares/errors/global-error";
export { mongoError } from "@middlewares/errors/mongo-error";
export { mutlerError } from "@middlewares/errors/multer-error";
export { zodError } from "@middlewares/errors/zod-error";
